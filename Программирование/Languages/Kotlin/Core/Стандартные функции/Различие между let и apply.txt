let передает объект-приемник в лямбду, но apply ничего не передает.
Также apply возвращает текущий объект-приемник, как только анонимная функция
завершает работу. let, напротив, возвращает результат выполнения последней
строки лямбды.