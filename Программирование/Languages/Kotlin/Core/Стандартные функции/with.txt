with - это разновидность run. В отличии от стандартных функций, with требует,
чтобы объект-приемник передавался ей в первом аргументе, а не как субъект вызова,
как это принято в других стандартных функциях.

val nameTooLong = with("Polarcubis, Supreme Master of NuetHack") {
	length >= 20
}